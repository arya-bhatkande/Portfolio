name: Deploy to Local Apache

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: |
          cd /var/www/myproject
          git pull origin main

      - name: Reload Apache
        run: sudo service apache2 reload

